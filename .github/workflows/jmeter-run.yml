name: JMeter_run

on:
  push:
    branches: [ main ]
  pull_request:
    branches: [ main ]
  workflow_dispatch:

jobs:
  run-jmeter:
    runs-on: ubuntu-latest

    env:
      JMETER_HOME: ${{ github.workspace }}/apache-jmeter-5.6.3

    steps:
      - name: Checkout code
        uses: actions/checkout@v3

      - name: Install Java
        run: |
          sudo apt-get update
          sudo apt-get install -y openjdk-17-jdk unzip

      - name: Download and Setup JMeter
        run: |
          curl -sL https://downloads.apache.org/jmeter/binaries/apache-jmeter-5.6.3.zip -o jmeter.zip
          unzip -q jmeter.zip
          chmod +x apache-jmeter-5.6.3/bin/jmeter

      - name: Create Results Directory
        run: mkdir -p Results/tmp

      - name: Run JMeter Test Plan
        run: |
          export PATH="$JMETER_HOME/bin:$PATH"
          jmeter -n \
            -t $JMETER_HOME/bin/examples/CSVSample.jmx \
            -l Results/tmp/results.jtl \
            -Jjmeter.save.saveservice.output_format=csv \
            -Jjmeter.save.saveservice.timestamp_format=ms \
            -Jjmeter.save.saveservice.default_delimiter=, \
            -Jjmeter.save.saveservice.assertion_results=none \
            -Jjmeter.save.saveservice.bytes=true \
            -Jjmeter.save.saveservice.label=true \
            -Jjmeter.save.saveservice.latency=true \
            -Jjmeter.save.saveservice.response_code=true \
            -Jjmeter.save.saveservice.response_message=true \
            -Jjmeter.save.saveservice.successful=true \
            -Jjmeter.save.saveservice.thread_name=true \
            -Jjmeter.save.saveservice.time=true \
            -Jjmeter.save.saveservice.connect_time=true

      - name: Generate HTML Report
        run: |
          export PATH="$JMETER_HOME/bin:$PATH"
          jmeter -g Results/tmp/results.jtl -o Results/tmp/html-report || echo "⚠️ HTML Report skipped."

      - name: Timestamp results folder
        run: |
          TIMESTAMP=$(date +"%Y-%m-%d_%H-%M-%S")
          if [ -d Results/tmp ]; then
            mkdir -p Results/$TIMESTAMP
            mv Results/tmp/* Results/$TIMESTAMP/ || echo "⚠️ Nothing to move."
            rm -rf Results/tmp
          else
            echo "⚠️ No tmp results folder found"
          fi

      - name: Upload Results Folder
        uses: actions/upload-artifact@v4
        with:
          name: jmeter-artifacts
          path: Results/
